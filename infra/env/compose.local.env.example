# Copy to an untracked file and pass it with: docker compose --env-file <path> ...
LOCAL_POSTGRES_PASSWORD=change-me-local-only
LOCAL_MINIO_ROOT_USER=change-me-local-only
LOCAL_MINIO_ROOT_PASSWORD=change-me-local-only
LOCAL_S3_BUCKET=spacey-replays
